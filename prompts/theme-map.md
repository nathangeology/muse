You are given a set of thematic labels and a fixed list of canonical themes. Your job: assign each label to exactly one canonical theme.

Print one line per label: "original label -> canonical theme". Every input label must appear. Use only theme names from the provided list. If a label doesn't fit any theme well, assign it to the closest one.
